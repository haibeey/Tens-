<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:padding="10dp"
    android:layout_height="wrap_content">
    <com.google.android.material.imageview.ShapeableImageView
        android:layout_width="250dp"
        android:id="@+id/image_button"
        android:layout_gravity="center"
        android:background="@android:color/transparent"
        android:scaleType="centerCrop"
        android:layout_height="100dp"
        android:layout_margin="2dp"
        android:contentDescription="@string/todo" />
    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/video_name"
        android:layout_width="wrap_content"
        android:layout_gravity="start|bottom"
        android:textSize="16sp"
        android:textColor="@color/colorWhite"
        android:padding="2dp"
        android:layout_alignBottom="@+id/image_button"
        android:layout_height="wrap_content" />

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/video_size"
        android:layout_width="wrap_content"
        android:textSize="12sp"
        android:layout_gravity="start|top"
        android:textColor="@color/colorWhite"
        android:padding="2dp"
        android:layout_alignTop="@+id/image_button"
        android:layout_height="wrap_content" />
    <ImageView
        android:layout_width="wrap_content"
        android:layout_gravity="end|top"
        android:id="@+id/vid_selected"
        android:layout_alignRight="@+id/image_button"
        android:background="@drawable/video_item_selected"
        android:layout_height="wrap_content"
        android:padding="2dp"
        android:layout_alignEnd="@+id/image_button"
        android:contentDescription="@string/todo" />

</RelativeLayout>